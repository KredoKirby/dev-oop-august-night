Actions folder  = all action files ~~ "if(isset)" code

Assets folder   = all your CSS, images, and javascript files

Classes folder  = all your classes

Views folder    = all view/display files = User Interface (UI), HTML, BOOTSTRAP

---------------------------------------------------------------------------------

    C = CREATE          ~~ INSERT INTO  ~~ Registration
    R = Retrieve/Read   ~~ SELECT       ~~ Login and Dashboard
    U
    D